"""Notifications & Communications bounded context (context #19, Sprint 10).

Channel provider ports/adapters and the event-consumer handler live here; the
ORM models, repositories, service, schemas, and routes follow the platform's
standard package layout (``app.models``, ``app.repositories``, etc.).
"""
