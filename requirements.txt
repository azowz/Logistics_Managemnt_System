# Mesaar backend runtime dependencies (Python 3.11+).
fastapi==0.111.0
uvicorn[standard]==0.30.1
SQLAlchemy==2.0.31
pydantic==2.8.2
pydantic-settings==2.3.4
email-validator==2.2.0
alembic==1.13.2
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 (unmaintained) is incompatible with bcrypt >= 4.1: its first-use
# self-test probes with a >72-byte secret, which newer bcrypt rejects with
# "ValueError: password cannot be longer than 72 bytes" on every hash/verify.
bcrypt==4.0.1
python-multipart==0.0.9

# Observability, background work, and infrastructure
loguru==0.7.2
prometheus-client==0.20.0
celery==5.4.0
redis==5.0.7
gunicorn==22.0.0

# Dev / test
pytest==8.2.2
httpx==0.27.0
